"""Given a set of distinct integers (between 1 and 15 inclusive) (ex: {3,6,2,1}), return an array of
all possible subsets that can be formed using the elements in the set. This also includes the empty set
and the entire set itself. The set of all such subsets is referred to as the powerset."""

def powerset_generator(elements):
  # put your code here; remove "pass"
  pass

